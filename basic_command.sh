update :
# add all changes to the staging area
git add .
# commit the changes with a message
git commit -m "Your commit message here"
# git push the changes to the remote repository
git push origin main <remote_branch_name>